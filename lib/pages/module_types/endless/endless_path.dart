
import 'package:flutter/material.dart';

class EndlessPath extends StatelessWidget {
  const EndlessPath({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Endless Path'),
      ),
      body: Center(
        child: Text('Endless Path'),
      ),
    );
  }
}
import 'package:flutter/material.dart';

class NoteModules extends StatelessWidget {
  const NoteModules({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Note Modules'),
      ),
      body: Center(
        child: Text('Note Modules'),
      ),
    );
  }
}